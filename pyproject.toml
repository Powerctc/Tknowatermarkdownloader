[project]
name = "tknowatermarkdownloader"
version = "0.1.0"
description = "TikTok Watermark Downloader Bot on Vercel"
requires-python = ">=3.9"
dependencies = [
    "pyTelegramBotAPI",
    "requests",
    "Flask"
]

[tool.vercel]
entrypoint = "api.webhook:app"
